apiVersion: v1
kind: Service
metadata:
  name: {{ include "metra-predictor.fullname" . }}
  labels:
    {{- include "metra-predictor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metra-predictor.selectorLabels" . | nindent 4 }}
