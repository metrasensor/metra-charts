apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metra-predictor.fullname" . }}-test-connection"
  labels:
    {{- include "metra-predictor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metra-predictor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
