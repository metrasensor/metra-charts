apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aiops-cassandra.fullname" . }}-test-connection"
  labels:
    {{- include "aiops-cassandra.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aiops-cassandra.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
