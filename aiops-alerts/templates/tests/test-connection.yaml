apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aiops-alerts.fullname" . }}-test-connection"
  labels:
    {{- include "aiops-alerts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aiops-alerts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
