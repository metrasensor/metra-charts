apiVersion: v1
kind: Service
metadata:
  name: {{ include "dec.fullname" . }}
  labels:
    {{- include "dec.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dec.selectorLabels" . | nindent 4 }}
