apiVersion: v1
kind: Service
metadata:
  name: {{ include "metra-django.fullname" . }}
  labels:
    {{- include "metra-django.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metra-django.selectorLabels" . | nindent 4 }}
