apiVersion: v1
kind: Service
metadata:
  name: {{ include "metra-alerts.fullname" . }}
  labels:
    {{- include "metra-alerts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metra-alerts.selectorLabels" . | nindent 4 }}
