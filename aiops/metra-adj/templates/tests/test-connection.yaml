apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metra-adj.fullname" . }}-test-connection"
  labels:
    {{- include "metra-adj.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metra-adj.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
