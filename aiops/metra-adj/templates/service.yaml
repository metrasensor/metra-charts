apiVersion: v1
kind: Service
metadata:
  name: {{ include "metra-adj.fullname" . }}
  labels:
    {{- include "metra-adj.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metra-adj.selectorLabels" . | nindent 4 }}
