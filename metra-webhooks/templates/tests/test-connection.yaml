apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metra-webhooks.fullname" . }}-test-connection"
  labels:
    {{- include "metra-webhooks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metra-webhooks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
