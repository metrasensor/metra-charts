apiVersion: v1
kind: Service
metadata:
  name: {{ include "metra-webhooks.fullname" . }}
  labels:
    {{- include "metra-webhooks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metra-webhooks.selectorLabels" . | nindent 4 }}
